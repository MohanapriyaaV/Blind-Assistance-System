name: blind_assist_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔥 Firebase Core (REQUIRED)
  firebase_core: ^3.6.0

  # 🔐 Authentication
  firebase_auth: ^5.3.0

  # 🗄️ Cloud Firestore
  cloud_firestore: ^5.4.0

  # 🔔 Push Notifications
  firebase_messaging: ^15.1.0

  flutter_map: any
  geolocator: ^12.0.0
  latlong2: any
  firebase_database: ^11.3.10
  flutter_tts: ^4.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
